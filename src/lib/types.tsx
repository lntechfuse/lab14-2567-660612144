export interface TermsAndConditionsProps {
  opened: boolean;
  close: () => void;
}
export interface footerProps {
  year: string;
  fullName: string;
  studentId: string;
}
export interface commentProps{
  name: string;
  rating: number;
  comment: string;
}